src  gibsrc gibdir gib.bundle/manifest.txt

def gibutil
out gib.$(name)
dep gib.bundle/$(name).c
cmd $(cc) $(cflags) -o $(out) $(srcdir)/gib.bundle/$(name).c

let name wrapcc
use gibutil
dep gib.bundle/reader.h
dep gib.bundle/writer.h
dep gib.bundle/common.h

let name nochange
use gibutil

let name findsrc
use gibutil
